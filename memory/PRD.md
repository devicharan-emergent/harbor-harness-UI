# ACM - Agent Configuration Manager

## Original Problem Statement
Build an Agent Configuration Manager (ACM) for managing AI agent configurations, running evaluations, and managing datasets.

## Core Requirements
- **Agent CRUD**: Full create, read, update, delete for agent configurations
- **Dual Data Source**: Switch between MongoDB (local) and Builder API (external)
- **Evaluation Suite**: Submit and monitor evaluation jobs against the Eval API
- **Dataset Management**: Full CRUD for evaluation datasets/problem statements
- **Multi-Eval Submission**: Submit batches of evals with mandatory group_id
- **Environment Switching**: Toggle between dev and ephemeral deployment environments
- **Google Authentication + per-user ownership**: Gate the app behind login; every eval/schedule request carries the user's UUID as `created_by`.

## Architecture
- **Frontend**: React + Tailwind CSS + Shadcn UI (port 3000)
- **Backend**: FastAPI + MongoDB (port 8001)
- **External APIs**: Eval API (harness-eval), Builder API (cortex-eph-builder)
- **Pattern**: Backend-for-Frontend (BFF) with proxy endpoints
- **State Management**: React Context (EnvContext, AuthContext)
- **Auth**: Emergent-managed Google OAuth; session token in `localStorage` (`acm_session_token`) sent as `?access_token=` query param on auth calls.
- **Ownership**: `created_by` (user UUID) injected via axios interceptor — query param on GET/DELETE, JSON body on POST/PUT/PATCH. Scoped to `/api/eval/jobs*`, `/api/eval/groups/*/jobs*`, and `/api/eval/scheduled-batches*`.

## Pages
1. Agent List (`/`), Agent Editor, Compare, Version History, Wizard
2. Eval Runs (`/evals`), Job Detail (`/evals/:id`)
3. Datasets (`/datasets`)
4. Schedules (`/schedules`, `/schedules/:id`, editor)
5. Auth: `/login`, `/auth/callback`

## What's Been Implemented
- [x] Dual-mode switching (MongoDB <-> Builder API)
- [x] Eval API integration + problem-statement display
- [x] Full Builder API CRUD
- [x] Dataset CRUD with problem-type wizards (incl. scratch_bench_phased multi-phase editor)
- [x] Multi-Eval Submission with mandatory group_id (moved to payload top level)
- [x] Grouped Eval Runs, Environment Switcher, Agent Deletion
- [x] Scheduled Batches CRUD + trigger + runs history
- [x] ScheduleDetail Analytics: SummaryKPIs, PhaseHeatmap, per-problem time series, sortable ProblemLeaderboard, Sparkline component
- [x] Run-Evaluation 3-step wizard with `agent_name` free-text + existence check via `/cortex/agents/exists`
- [x] Bug-bench image_available indicator (green/red dot)
- [x] **Google Authentication** (Feb 2026) — Emergent-managed OAuth, login page, protected routes, user menu with logout
- [x] **Per-user ownership** (Feb 2026) — `created_by` UUID threaded through every relevant request via centralised axios interceptor (`src/services/apiHelpers.js`) and backend proxy pass-through
- [x] **Runtime same-origin API baseURL** (Feb 2026) — `src/services/apiBase.js::getApiBaseURL()` falls back to `window.location.origin` when the served page origin differs from `REACT_APP_BACKEND_URL`, avoiding the preview 307 cross-origin trampoline.
- [x] **EvalRuns filter bar** (Feb 2026) — batch-name search + independent agent / prompt / date-range filters with AND/OR combine toggle, active-filter chips, clear-all, and empty-state copy (`src/components/evals/EvalFilterBar.js`).
- [x] **Cortex Agents editor** (Feb 2026) — `/cortex/agents` page: eph gate + agent list + Monaco YAML editor with quick-fields strip + comment-preserving edits (`eemeli/yaml`). v1.1 polish: schema-aware Monaco (`monaco-yaml` + `agentSchema.json`, blob-shim same-origin worker, opt-out flag `acm_cortex_disable_yaml_lsp`), diff-before-save modal, dirty-state guard + `beforeunload`, located server-error validation (Monaco marker + quick-field red border via `locateServerError.js`), delete-with-consequence-copy, provider-aware `model.id` combobox, list search/sort, optimistic delete + Undo toast, Cmd/Ctrl+S + Esc, scoped health chip (hidden on `/cortex/*`), focus-revalidate eph gate, error boundary around the editor.
- [x] **Eph-driven eval submission** (Feb 2026) — `EphPicker` (shared component) at the top of `RunEvalModal` step 2 with live DB / emergent / cortex readiness badge gating Next + Submit. Free-text `cortex_url` hidden by default; resurfaced via `?advanced=1`. Backend stub `/api/eval/cortex/ephs/{eph}/readiness` keyed off eph name (`cluster3-test` → emergent:false) until harness ships the real endpoint; submission routes via new `/api/eval/jobs-with-es` proxy.
- [x] **Testing Agent Bench dataset type + fork-eval flow** (Feb 2026 — fork iter 21) — Added `testing_agent_bench` to `DATASET_TYPE_OPTIONS` in `DatasetEditorModal.js`. Step 1 relabels "Instance ID" → "Production Job ID" with prod-fork helper text. Step 2 renamed "Task & Golden" — replaces Phases/Tests UI with two required textareas: HITL Input (→ `problem_statement`) + Golden Output (→ `natural_language_tests`). Step 3 collapses to Agent Name (required, in `attributes.agent_name`) + Model Name (optional, omitted if blank); `attributes.prod_job_id` mirrors `instance_id`. Create payload omits `problem_set_ids` and sets `name = "${dataset_type}/${instance_id}"`. RunEvalModal: new `isTestingAgentMode` / `hasMixedTypes` memos hide Target eph picker, Template, batch agent override (the legacy one), Resources (CPUs/Memory/Storage), Experiment Config when ALL selected problems are testing_agent_bench — only Group Run ID + User ID + new optional "Agent Name (override)" remain. Mixed selections (some testing_agent_bench + some scratch/bug) blocked at Step 1 with a rose warning banner; Next disabled. Submit branches: testing_agent_mode loops one POST per problem to `/api/eval/testing-agent-evals`, hydrating dataset via `getDatasetForProblem` if list endpoint trimmed the body, and per-eval body uses `agentNameOverride.trim() || attrs.agent_name` (override wins for A/B testing). Backend adds `proxy_submit_testing_agent_eval` route pass-through to `${EVAL_API_BASE}/api/v1/testing-agent-evals`. `attachOwnership` regex extended to inject `created_by` on the new endpoint. Verified end-to-end: dataset creation 200 with `version:1, is_active:true`; eval submission 200 with `{jobs:[{id, problem, status:'queued', k8s_job_name, created_at}]}`. Wizard test-ids: `dataset-hitl-input-textarea`, `dataset-golden-output-textarea`, `eval-testing-agent-name-override`, `testing-agent-mode-banner`, `step1-mixed-types-warning`.
- [x] **Testing Agent Bench `model_name` picker + per-run override** (Feb 2026 — fork iter 21) — New shared `ModelNamePicker` (`/app/frontend/src/components/evals/ModelNamePicker.js`) — Select dropdown with `(default)` sentinel + 5 presets (`claude-sonnet-4-5`, `claude-opus-4-7`, `claude-opus-4-8`, `claude-sonnet-4-6`, `claude-fable-5`) + `Custom…` swap-to-Input for free-text. Used in two places: (1) DatasetEditorModal Step 3 Model Name field — persists to `attributes.model_name`, blank = omitted from payload, value round-trips on edit. (2) RunEvalModal Step 2 testing_agent_mode — new `modelNameOverride` state + `modelOverrideTouched` flag; effect pre-fills from `selectedProblems[0].attributes.model_name` on entering Step 2 (only if user hasn't touched the field). In `handleSubmit` per-problem branch: if user touched the field, `modelNameOverride.trim()` wins (blank → key omitted); otherwise fall back to `attrs.model_name`. Verified via Playwright + network intercept: scenario A (untouched + prefilled) sends `model_name: "claude-opus-4-8"`; scenario B (override to preset) sends `model_name: "claude-sonnet-4-5"`; scenario C (cleared to default) submits with `model_name` key absent from body. Dataset record is NEVER mutated. Test-ids: `attr-model-name-select`, `attr-model-name-custom`, `eval-testing-model-override-select`, `eval-testing-model-override-custom`.
- [x] **Testing Agent Bench batched submit, judge-config singleton, JobDetail raw card** (Feb 2026 — fork iter 22 + 23). **Iter 22** refactored RunEvalModal `handleSubmit` testing_agent branch into ONE batched POST with `items[]` (instead of one per dataset, which 409'd on duplicate `group_run_id`). Always uses items[] even for 1-element selection. **Iter 23** added (a) Mongo `judge_config` singleton (`_id="default"`) — `GET/PUT/POST /api/eval/judge-config[/reset]` with server-side validation enforcing `{golden}` + `{candidate}` tokens in the prompt. (b) Default prompt + `gemini-flash-latest` model defined in `server.py`; GET returns them with `is_default:true` until a doc is saved. (c) New `JudgeConfigDialog` component — textarea + model dropdown (5 presets + Custom…), live token-presence badges, Save disabled until both tokens present, Reset-to-Default button. (d) RunEvalModal Step 2 testing_agent_mode renders a summary card showing model + prompt length + `default` badge, with an "Edit judge prompt & model" button opening the dialog. (e) `handleSubmit` stamps `judge_prompt` + `judge_model` at the top level of the batched body. (f) JobDetail page renders `llm-judge-raw-card` whenever any `phase_results[].judge_raw_request` or `judge_raw_response` is present — each phase block has two `Collapsible` sections (Raw Request / Raw Response) with monospace `<pre>`, scrollable, and Copy buttons. Card hidden entirely for older scratch_bench / non-testing-agent runs. Test-ids: `judge-config-summary`, `open-judge-config`, `judge-config-dialog`, `judge-prompt-textarea`, `judge-model-select`, `judge-model-custom`, `judge-config-save`, `judge-config-cancel`, `judge-config-reset`, `llm-judge-raw-card`, `judge-raw-block-<idx>`, `judge-raw-{request|response}-{toggle|content|copy}-<idx>`. **Iter 23 testing**: 6/6 backend pytest + 14/14 frontend Playwright scenarios passed; harness `items[]` handler confirmed shipped (iter-22 upstream blocker resolved); scratch_bench regression clean (no judge keys leak into `/jobs` body).
- [x] **Dedicated LLM Judge page + 2-option model dropdown** (Feb 2026 — fork iter 24). Refactored `JudgeConfigDialog.js` to extract a reusable `JudgeConfigForm` shared by both the existing Run Eval modal dialog AND a new standalone `/judge-config` page (`/app/frontend/src/pages/JudgeConfigPage.js`). New sidebar entry `LLM Judge` (lucide `Scale` icon) added to `AppShell.navItems` between Datasets and Compare. Route registered in `App.js`. Model dropdown trimmed to **exactly 2 options**: `gemini-flash-latest` (default) and `gpt-5.5`. No `Custom…` option, no free-text input on either surface. Backward compat: if a stored doc has a non-allowed model (e.g. legacy `claude-sonnet-4-5`), the picker silently coerces the displayed value to the fallback (`gemini-flash-latest`) without mutating the server doc until Save is clicked. Both surfaces share state via the singleton GET — saving on one immediately reflects in the other on reopen. **Iter 24 testing**: 11/11 frontend Playwright scenarios passed (sidebar nav, page render, 2-option dropdown, validation gating, dialog↔page state sharing, batched submit still stamps judge config, scratch_bench regression clean, backward-compat with stale model). Reviewer left one optional UX nit (inline hint when stored model not in allowed list); not blocking.
- [x] **RunEvalModal "Select all (N)" + "Number of Runs"** (Feb 2026 — fork iter 25). Step 1: new `Select all (N)` button next to the search bar (toggle to `Clear (N)` when all visible are selected); scoped to the filtered list; mixed-type guard toasts when filtered list spans testing_agent_bench + others. Step 2: new `Number of Runs` integer input (default 1, min 1, max 10, clamped). `handleSubmit` refactored to loop 1..N — each run gets a distinct `group_run_id` suffix `-run-1`, `-run-2`, … only when N>1 (single-run wire format preserved). Submit button reads `Submit (X × N = Y jobs)` and live `Submitting K/N…` progress; final toast `Submitted Y job(s) across N run(s)`. Pre-hydrates testing_agent items once before the loop to avoid N×problems dataset fetches. judge_prompt/judge_model stamped on every iteration. Mid-run failures throw out of the loop without rollback. **Iter 25 testing**: 15/15 frontend Playwright scenarios passed.
- [x] **Inline Cancel button on /evals job rows** (Feb 2026 — fork iter 26). New `CancelJobButton` component in `EvalRuns.js` rendered between StatusBadge and ScoreBadges on each job row. Visible only for `queued|generating|running`; renders nothing otherwise. Ghost h-7/w-7 with Ban icon and rose-600 text; aria-label `Cancel job <8charPrefix>` + title `Cancel this job`; data-testid `cancel-job-<jobId>`. Click stops row navigation, prompts via `window.confirm`, then DELETEs `/api/eval/jobs/{id}` via existing `cancelEvalJob`. Loader2 spinner during the call; success toast + `fetchJobs()` refresh on 2xx; parsed-error toast on 4xx/5xx with the button reverting to enabled. **Iter 26 testing**: 11/11 frontend Playwright scenarios passed. Reviewer flagged unrelated pre-existing backend 500s on `GET /api/eval/jobs/aggregate` + `GET /api/eval/groups/{id}/jobs` for some `TEST_iter25_*` group ids — out of scope, UI degrades gracefully.
- [x] **Fix LLM-Judge raw card field-name mismatch** (Feb 2026 — fork iter 27). The card on JobDetail (`llm-judge-raw-card`) was wired in iter 23 to read `phase_results[].judge_raw_request` / `judge_raw_response`, but the harness actually writes those fields as plain `raw_request` / `raw_response` on each phase result — so the card never showed for any real run. Changed the aggregator in `JobDetail.js` to read the correct field names. Verified end-to-end on job `27cc9208-…` (testing_agent_bench): card now renders with `1 phase` badge, collapsed Raw Request (6318 chars) + Raw Response (557 chars) rows, per-section Copy buttons, and the full prompt + JSON judge output inside the `<pre>` blocks when expanded. UI stays uncluttered — collapsed by default, expanded blocks scroll inside `max-h-[400px]`.
- [x] **Verifier Config — per-bench prompts + models** (Feb 2026 — fork iter 28). Generalized the singleton "LLM Judge" config into a per-bench "Verifier Config" surface. Backend: new `GET/PUT/POST /api/eval/verifier-config?bench=<testing_agent_bench|scratch_bench_phased>` routes backed by `judge_config` collection with `_id=<bench_type>`. `BENCH_VERIFIER_DEFAULTS` carries per-bench defaults + required tokens (`{golden}`+`{candidate}` for testing-agent, `{preview_url}`+`{test_case}` for scratch). Legacy `/eval/judge-config` retained as alias to bench=testing_agent_bench. Frontend: sidebar entry renamed to **"Verifier Config"** (route still `/judge-config`). Page rebuilt with a bench dropdown at the top swapping the form between the two benches independently; each maintains its own customization. `JudgeConfigDialog.js` refactored to export `BENCH_META`, `VerifierConfigForm`, `VerifierConfigDialog` (+ legacy aliases). Model select restored to **3 options: `gemini-flash-latest`, `gpt-5.5`, `Custom…`** (free-text input on Custom). Per-bench labels (Judge Prompt/Model vs Browser Prompt/Model), header copy, token chips and footer helper. RunEvalModal lazy-loads the correct verifier on Step 2 entry. Stamping logic gated on `!is_default` — testing_agent submit only includes top-level `judge_prompt`/`judge_model` when customized; scratch submit only stamps `experiments.browser_prompt`/`browser_model` on each `scratch_bench_phased` eval when customized. **Iter 28 testing**: 13/13 backend pytest (new `/app/backend/tests/test_verifier_config.py`) + 100% frontend Playwright assertions passed; verified both omit-when-default AND stamp-when-customized paths via network interception. Reviewer flagged two non-blocking nits (RunEvalModal.js now 1436 lines — refactor candidate; one-shot legacy doc migration could simplify reasoning).
- [x] **Monaco editor cross-origin "Script error." crash fixed** (Feb 2026 — fork) — `@monaco-editor/react` was loading `monaco-editor` from the public CDN (jsdelivr) via the AMD loader; any error in that cross-origin script surfaced as opaque "Script error." with no message/filename/lineno, uncatchable by try/catch. Rewrote `lib/agentMonaco.js` to expose `bootstrapMonacoLoader()` which (a) installs a same-origin inline noop Web Worker as `MonacoEnvironment.getWorker` and (b) lazy-imports the locally-bundled `monaco-editor` and calls `loader.config({ monaco })` + `loader.init()` to redirect `@monaco-editor/react` away from the CDN. `AgentEditor` gates the `<Editor>` render on the bootstrap promise. Verified end-to-end: editor mounts cleanly, console logs `[acm] monaco bound to local monaco-editor (no CDN, no cross-origin workers)`, zero Script-error events. The earlier `monaco-yaml` LSP schema-validation path stays a no-op (opt-in re-enable is future work — separate issue).
- [x] **"Open in eval" deep link from Cortex Agents → RunEvalModal** (Feb 2026 — fork) — Edit-mode `AgentEditor` now has an "Open in eval" button (rocket icon) that navigates to `/evals?run=1&eph=<eph>&agent=<agent_id>`. `EvalRuns` consumes those query params on mount, auto-opens `RunEvalModal` with `initialEph` + `initialAgentName` props, then strips the params from the URL (`replace: true`) so closing + reopening starts clean. `RunEvalModal` seeds `submitEph`, `ephName`, and `agentNameOverride` from those props in its open-reset effect. Unsaved-edits guard (`window.confirm`) before navigating since `beforeunload` doesn't catch React Router transitions. Verified end-to-end: created `dl_test_agent_v1` on `cluster3-test`, clicked Open in eval → modal Step 2 pre-filled with both fields, zero Script-error events.
- [x] **CSV dataset import + export on `/datasets`** (Feb 2026 — fork iter 29). Backend BFF: `POST /api/eval/datasets/import` (multipart CSVs → harness `/api/v1/datasets/bulk`, dedup by `(dataset_type, instance_id)`), `GET /api/eval/datasets/export` (per-type CSV stream w/ `Content-Disposition`, optional repeated `instance_id` query for subsetting; round-trip-compatible with import headers), `GET /api/eval/datasets/template` (per-type starter CSV with 1–2 filled example rows for all 5 supported types). Frontend: new `ImportCsvDialog` (type picker + Download template + multi-file picker + collapsible per-type Column legend lifted from spec Part 6b + result banner showing `created/skipped/errors[index,instance_id,error]`), header-bar `Import CSV` + `Export (N)` buttons (Export disabled until ≥1 row checked per user choice 2a), checkbox column with select-all (header indeterminate state) + selection chip with clear, `DatasetsPage` `DATASET_TYPES` filter expanded to all 5 types (added `testing_agent_bench`, `wingman_bench`). After successful import the dialog stays open with the result banner but the file list is cleared (no accidental re-upload) and the parent auto-switches the type filter to the imported type (user choice 3a). Multi-type export triggers one CSV download per dataset_type. BFF maps upstream 500 on single-instance lookup → 404 so `/export?instance_id=<missing>` surfaces cleanly. New helpers `importDatasetsCsv`, `exportDatasetsCsv`, `getDatasetTemplateCsv`, `triggerBlobDownload` in `evalApi.js`. Test-ids: `datasets-import-csv-btn`, `datasets-export-csv-btn`, `datasets-select-all`, `dataset-select-<iid>`, `datasets-selection-summary`, `datasets-clear-selection-btn`, `import-csv-dialog`, `import-dataset-type-select`, `import-download-template-btn`, `import-legend-toggle`/`import-legend-panel`, `import-file-input`/`import-file-list`/`import-file-row-<i>`/`import-file-remove-<i>`, `import-cancel-btn`/`import-upload-btn`, `import-result-banner`/`import-result-errors`/`import-error-row-<i>`/`import-error-banner`. **Iter 29 testing**: 15/15 backend pytest (new `/app/backend/tests/test_dataset_csv.py`) + 16/16 frontend Playwright steps (incl. multi-type export, auto-type-switch, bad-row error rendering, re-upload skip) passed. Reviewer left non-blocking polish suggestions (parallelise multi-instance export awaits, tooltip-on-disabled discoverability).
- [x] **Remove Target eph / User ID inputs + Comments feature** (Feb 2026 — fork iter 34). User asked to remove three sections that were no longer needed: (a) the "Target eph" picker block in `RunEvalModal` Step 2 (Label + EphPicker + 'Check readiness temporarily disabled' warning); (b) the "User ID" UUID input block in Step 2 — `userId` is now derived automatically from `useAuth().user.user_id` so the harness still gets a `user_id` on submit but the field is no longer editable; (c) the entire **group-run Comments feature** added in iter30 — `GroupCommentsPanel.js` deleted, three `evalApi.js` helpers (`listGroupComments`/`createGroupComment`/`deleteGroupComment`) removed, three backend routes (`GET/POST/DELETE /api/eval/groups/{group_id}/comments`) deleted from `server.py`, Mongo `group_run_comments` collection dropped via Motor. **State preserved**: `submitEph` + `submitEphReadiness` state are kept (just the UI is gone) so the `/cortex` Open-in-Eval deep-link (`?run=1&eph=…&agent=…`) still seeds the eph value silently for the POST body. **Testing verdict (iter 34)**: 100% on removal asserts — backend curl shows 3× 404 on the deleted routes, mongo collection no longer in the listing, GroupCommentsPanel.js absent from filesystem, `eval-user-id` test-id absent from DOM, "Target eph" label + Check-readiness button absent from DOM (40 groups checked). No-regression: Mine-only filter still issues `created_by=TEST_pw_user@emergent.com` correctly on toggle ON and strips it on toggle OFF.
- [x] **`created_by` injection bug + Mine-only filter + group-run comments** (Feb 2026 — fork iter 30→31). User reported (1) `created_by` not sent in eval POST body, (2) wanted a "Mine only" filter on /evals, (3) wanted comments on each group run. **Root cause #1**: `apiHelpers.attachOwnership` had a stray `return config;` on line 42 that short-circuited the entire injection — the whole body of the interceptor was dead code. Fix: removed the stray return; also scoped the interceptor to write methods only (POST/PUT/PATCH) — GET/DELETE no longer ride with `created_by` automatically, which previously made the Mine-only toggle indistinguishable at the network layer. **Mine-only filter**: new `filter-mine-only-toggle` Button in `EvalFilterBar`, `mineOnly` field added to `EMPTY_FILTERS`, `buildJobFilter(filters, currentUserId)` enforces ownership predicate client-side, `EvalRuns.fetchJobs` passes `created_by` server-side when the toggle is on so the filter works across pagination. **Comments**: new Mongo collection `group_run_comments` (`{comment_id, group_id, text, created_by_user_id, created_by_email, created_by_name, created_at, updated_at}`), three routes `GET/POST/DELETE /api/eval/groups/{group_id}/comments[/{comment_id}]` — GET is open (read-only), POST + DELETE require `_get_session_user`, DELETE enforces author-only with 403. New FE component `GroupCommentsPanel.js` mounted inside the expanded `Collapsible` of each non-`_ungrouped` group on `/evals`; supports list / post / delete with Cmd+Enter shortcut, optimistic updates, and a Trash icon shown only on own comments. **Also fixed in iter31**: `evalApiClient` previously had no auth-token interceptor → comments POST/DELETE always 401 from the UI even with a valid session token. Added the same `?access_token=<acm_session_token>` request interceptor that `cortexApi.js` uses, so all protected `/api/eval/*` routes authenticate. **Testing**: iter30 found two regressions (always-on `created_by` qp, missing auth on evalApiClient); both fixed in iter31 and re-tested — **5/5 frontend Playwright scenarios PASS** including POST `/api/eval/jobs-with-es` body carrying `created_by`, Mine-only toggle ON/OFF re-issuing GET with/without `created_by` qp, comments POST 200, comments DELETE 200. Test-ids: `filter-mine-only-toggle`, `group-comments-<groupId>`, `group-comments-empty-<groupId>`, `group-comments-list-<groupId>`, `group-comment-input-<groupId>`, `group-comment-submit-<groupId>`, `group-comment-<commentId>`, `group-comment-delete-<commentId>`.
- [x] **CSV import bug fix + spec re-alignment** (Feb 2026 — fork iter 30). User reported "import dataset is not working properly". **Root cause**: `evalApi.js::importDatasetsCsv` set `Content-Type: 'multipart/form-data'` explicitly, which strips the axios-generated `; boundary=…` parameter — FastAPI then couldn't parse the multipart body. **Fix**: pass `Content-Type: undefined` to clear the instance default and let axios+browser emit the correct multipart Content-Type with boundary. Also re-aligned the surface to a fresh handoff spec the user uploaded: (1) backend `/import` returns `JSONResponse(resp.status_code, payload)` so per-row `errors[]` aren't nested under `detail`; (2) `/export` adds `dataset_type=all` → ZIP with one CSV per known type, helpers extracted (`_serialize_datasets_to_csv`, `_fetch_datasets_for_export`); (3) FE helpers renamed `importDatasetsCSV` / `exportDatasetsCSV` (caps), `exportDatasetsCSV` self-triggers download, blob-error JSON shim added; (4) `ImportCsvDialog` replaced with `ImportDatasetsModal` (simpler test-ids `import-datasets-dialog`, `import-type-select`, `import-pick-files-btn`, `import-add-more-btn`, `import-submit-btn`, `import-close-btn`, `import-error-banner`, `import-result`, `import-{created|skipped|errors}-badge`, client-side header-only template); (5) `DatasetsPage` gets **Export All (zip)** button next to **Export Selected**, per-row Download icon as the first Actions icon, mixed-type selection guard with explanatory tooltip, selection auto-resets on `[typeFilter, page]` change. Test-ids renamed: `select-all-checkbox`, `select-row-<iid>`, `export-selected-btn`, `export-all-btn`, `import-datasets-btn`, `download-dataset-<iid>`. **Verified via Playwright**: import 2 → created: 2; re-upload → skipped: 2; Export Selected/per-row/Export All all download with server-supplied filenames (`bug_bench_<iid>.csv` and `datasets_export.zip`). Curl: missing iid → 404, `all` + iid → 400.

## Testing Status (Iteration 19 – Feb 2026)
- Backend: 6/6 auth + **11/11 created_by pass-through** tests pass (`/app/backend/tests/test_created_by_passthrough.py`)
- Frontend: RunEvalModal POST /api/eval/jobs now carries `created_by` in body. /evals + /schedules GETs carry `?created_by=…`. /datasets/cortex/stats/health clean. See `/app/test_reports/iteration_19.json`.

## Bug Fix (Feb 2026 — iter19)
Backend proxy endpoints were silently dropping `created_by` by rebuilding outbound payloads with a strict allowlist. Fixed in `server.py`:
- `proxy_submit_eval` (POST /eval/jobs) now forwards `created_by` in body.
- List/Get/Delete on `/eval/jobs`, `/eval/jobs/{id}`, `/eval/jobs/aggregate`, `/eval/groups/{id}/jobs`, `/eval/scheduled-batches*`, `/eval/scheduled-batches/{id}/runs` now accept `created_by: Optional[str]` and pass it through as a query param.
- `proxy_trigger_scheduled_batch` forwards its body wholesale.

## Prioritized Backlog
### P1
- Split `server.py` (~1100 lines) into `auth_routes.py`, `agent_routes.py`, `eval_proxy_routes.py`
- Dropdown metadata integration (Builder models/tools/prompts in editor)

### P2
- Add Jest/RTL tests for `components/analytics/*` (skipped previously)
- Theme picker dropdown (multiple palettes – Solarized, GitHub-like, etc.)
- Clone Eval Groups feature

### P3
- Command palette (Cmd+K)
- Batch operations on agent list
- Re-evaluate drag-and-drop (DnD-kit portal issue)
